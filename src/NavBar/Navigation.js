import React from 'react';
import NavLinks from './NavLinks';

const Navigation = () => {
    return (
        <div>
            <nav id='Navigation'>
                <NavLinks />
            </nav>
        </div>
    );
}

export default Navigation;
